# Bilibili But Higher IQ

> **灵感来源与致谢**
>
> 本项目在开发过程中参考了以下两个优秀项目：
> - **[BiliBili_Optimizer](https://github.com/Kyouichirou/BiliBili_Optimizer)** by Kyouichirou - 提供了视频评分系统、播放控制、Bayesian 分类器等功能的思路
> - **[bilibili-cleaner](https://github.com/festoney8/bilibili-cleaner)** by festoney - 提供了评论区多维度过滤、白名单机制、动态过滤等设计的参考
>
> 如需更丰富的功能（如视频评分、本地观看历史、下载命令生成等），可参考上述项目。
>
> ---

在互联网上，你对面的人可能是一个小学生。  
但我们为什么要把整个互联网，都做成“小孩桌”呢？

如果你已经受够了这些东西：

- 首页一眼望去，全是情绪钩子、标题党、低质搬运
- 评论区不是在讨论，是在复读、拱火、比烂
- 你明明想找点正常内容，却总被平台往最低注意力密度的地方拖

这个扩展就是拿来干这个的：**帮你把 B 站里的低质量噪音筛掉一点，让“正常内容”和“正常人说话”重新有机会被看见。**

它不是道德警察，也不是“我替你决定一切”的 AI 审判官。  
它更像一个**可配置的内容筛子**：你自己决定筛多狠，筛什么，谁该被拉黑。

---

## 这玩意儿能帮你什么

### 1. 过滤低质量视频

- 识别引战、标题党、同质化内容
- 对可疑内容执行`隐藏`或`淡化`
- 支持首页卡片和视频页推荐区

### 2. 过滤低质量评论

- 优先处理垃圾评论、拱火评论、低信息密度评论
- 支持“仍然显示”，不搞一刀切
- 可以直接从评论区一键屏蔽用户

### 3. 给你控制权，而不是替你发号施令

- 每类过滤都能单独开关
- 支持用户黑名单
- 支持自定义关键词
- 支持数据导入 / 导出
- 设置可通过 `chrome.storage.sync` 同步

---

## 现在修到哪了

当前项目已经完成这些核心能力：

- Manifest V3 扩展结构
- Popup / Options 设置界面
- 视频评分与评论过滤逻辑
- 跨标签页设置同步
- B 站新页面结构适配
- **Shadow DOM 评论区/组件适配**

如果你前几天看过这个仓库，最新一轮修复重点就是这里：  
**B 站现在大量用了 Web Components + Shadow DOM。评论区和部分卡片不能再靠平面 DOM 的老 selector 硬抓。**  
这轮已经补上了 Shadow DOM 穿透观察、评论数据提取、推荐卡片提取，以及对应的探针测试。

---

## 适合谁

适合这些人：

- 想把 B 站重新用回“信息工具”而不是“情绪老虎机”的人
- 不想每次刷首页都被低幼内容糊脸的人
- 希望评论区至少先过滤掉一层垃圾的用户
- 对“平台默认推荐=我真正想看”这件事已经没耐心的人

不适合这些人：

- 觉得越吵越有意思
- 觉得标题越炸裂越开心
- 觉得“互联网本来就这样，忍忍吧”就行

---

## 安装

### 开发者模式加载

1. 克隆仓库
2. 打开 Chrome / Edge 的 `chrome://extensions/`
3. 开启右上角“开发者模式”
4. 点击“加载已解压的扩展程序”
5. 选择本项目目录

---

## 怎么用

### 最快上手

1. 装好扩展
2. 打开 B 站
3. 点扩展图标
4. 打开“启用过滤”
5. 先用默认设置刷一会儿
6. 再根据你的容忍度决定是“淡化”还是“直接隐藏”

### 如果你想更狠一点

去 `Options` 页面：

- 调整评论过滤模式
- 增加自己的关键词
- 管理黑名单
- 导入/导出配置

### 如果你在评论区看到典型脏东西

1. 先点“仍然显示”确认内容
2. 直接点“屏蔽用户”
3. 这个用户后续内容会一起被过滤

---

## 项目结构

```text
├── manifest.json
├── background/
│   └── service-worker.js
├── content/
│   ├── dom-observer.js
│   ├── video-scorer.js
│   └── comment-filter.js
├── storage/
│   └── blocklist-manager.js
├── styles/
│   └── content.css
├── ui/
│   ├── popup/
│   └── options/
├── utils/
│   ├── constants.js
│   └── shadow-dom-utils.js
├── tests/
│   └── e2e/
└── _locales/
```

---

## 本地开发

```bash
npm run build
```

加载扩展后，常见调试入口：

- 后台：扩展详情页 -> `Service Worker`
- 内容脚本：页面 DevTools -> `Sources` -> `Content scripts`
- Popup：右键扩展图标 -> `Inspect popup`

---

## 测试

### 本地 Shadow DOM 探针

```bash
npm --prefix tests run probe:shadowdom
```

### 本地探针测试

```bash
npm --prefix tests run test:probe
```

说明：

- `shadow-dom-probe.mjs` 用来统计真实页面 / fixture 的 selector 命中情况
- `shadow-dom-integration.test.mjs` 用来验证 extractor 能不能从 Shadow DOM 组件宿主上拿到视频/评论数据

---

## 隐私说明

- 数据默认存本地浏览器
- 设置可经 `chrome.storage.sync` 同步
- 不上传你的浏览行为
- 不把内容数据发去第三方服务器

---

## 这项目的态度

这个项目背后的想法其实很简单：

> 平台可以越来越像“小孩桌”，但你的浏览器不一定非得陪着一起降级。

如果一个地方还能讨论、还能学习、还能认真表达，  
那它就值得一层最基本的“去噪工具”。

---

---

## 下一步开发计划

### 短期迭代（v0.2.x）

| 功能 | 描述 | 优先级 |
|------|-------|--------|
| 视频评分系统 | 用户可对视频打3/4/5星，高分视频优先展示 | P1 |
| 视频时长过滤 | 可设置屏蔽 X 秒以下的短视频 | P1 |
| 评论过滤增强 | 支持按用户等级、评论类型过滤 | P2 |
| 右键屏蔽视频 | 在视频卡片上右键可直接屏蔽 | P2 |

### 中期规划（v0.3.x）

| 功能 | 描述 | 优先级 |
|------|-------|--------|
| 播放控制增强 | 倍速播放（最多5x）、影院模式、灯光自动调节 | P2 |
| 动态/首页过滤 | 过滤首页推荐中的低质量内容 | P2 |
| 本地观看历史 | 记录看过的视频，标记已观看/想看 | P3 |
| 导入/导出配置 | 备份和恢复设置与黑名单 | P2 |

### 长期愿景（v1.0）

| 功能 | 描述 | 优先级 |
|------|-------|--------|
| BBDown 下载支持 | 生成下载命令，一键获取高清视频 | P3 |
| 云端配置同步 | 跨设备同步设置（基于 chrome.storage.sync） | P3 |
| 社区规则包 | 预设不同场景的过滤规则（技术/生活/游戏等） | P3 |

---

## 许可证

MIT，见 [`LICENSE`](LICENSE)。
